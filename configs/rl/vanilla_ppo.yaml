run_name: vanilla_ppo
scenario: base
smoke: false
seed: 20260623
environment:
  sequence_length: 30
  state_dim: 17
  action_dim: 3
algorithm:
  name: vanilla_ppo
  device: cuda
  lr: 0.0001
  gamma: 0.99
  gae_lambda: 0.95
  clip_epsilon: 0.2
  value_coef: 0.5
  entropy_coef: 0.01
  update_epochs: 4
training:
  episodes: 500
  update_every_steps: 10
outputs:
  root: experiments/rl
