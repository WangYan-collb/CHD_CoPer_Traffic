# Windows + NVIDIA GPU environment.
# Install PyTorch first with the CUDA wheel index, then install the common packages:
#
#   py -3.9 -m venv .venv
#   .venv\Scripts\python.exe -m pip install --upgrade pip
#   .venv\Scripts\python.exe -m pip install torch==2.1.0 --index-url https://download.pytorch.org/whl/cu121
#   .venv\Scripts\python.exe -m pip install -r requirements-windows-gpu.txt
#
# SUMO 1.25.0 must be installed separately and SUMO_HOME must point to its install directory.
numpy==1.26.3
pandas==1.5.3
pyyaml==6.0.1
matplotlib==3.7.1
sumolib==1.25.0
traci==1.25.0
